% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{readBedMethyl}
\alias{readBedMethyl}
\title{Read an ONT modkit bedMethyl file}
\usage{
readBedMethyl(
  bedmethyl,
  h5file,
  mod,
  chunk_size = 5e+06,
  check_sorted = TRUE,
  fields = c("coverage", "mod_reads")
)
}
\arguments{
\item{bedmethyl}{Path to a nanoporetech modkit bedMethyl file (optionally gzipped).}

\item{h5file}{Path to the HDF5 file to create.}

\item{mod}{Modification code to retain (e.g., \code{"m"}).}

\item{chunk_size}{Reserved for future use.}

\item{check_sorted}{Logical, check that records are sorted by chrom and chromStart.}

\item{fields}{Character vector of numeric fields to load. Defaults to \code{c("coverage", "mod_reads")}.}
}
\value{
An \code{RBedMethyl} object.
}
\description{
Create an \code{RBedMethyl} object backed by HDF5Array from a nanoporetech
modkit bedMethyl file (headerless).
}
\examples{
lines <- c(
  paste("chr1", 0, 1, "m", 0, "+", 0, 1, 0, 10, 0.5, 5, 5, 0, 0, 0, 0, 0, sep = "\t"),
  paste("chr1", 10, 11, "m", 0, "+", 10, 11, 0, 20, 0.25, 5, 15, 0, 0, 0, 0, 0, sep = "\t")
)
tmp <- tempfile(fileext = ".bed")
writeLines(lines, tmp)
h5 <- tempfile(fileext = ".h5")
bm <- readBedMethyl(tmp, h5, mod = "m", fields = c("coverage", "pct", "mod_reads"))
bm
}
